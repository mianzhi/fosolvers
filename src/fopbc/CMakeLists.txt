SET(fopbc_src
  fopbc.f90
  modPbc.f90
)

ADD_EXECUTABLE(fopbc ${fopbc_src})

TARGET_LINK_LIBRARIES(fopbc
  fosolvers
  ${BLAS_LIBRARIES}
  ${LAPACK_LIBRARIES}
  ${SUNDIALS_LIBRARIES}
  ${MATHEVAL_LIBRARIES}
  ${METIS_LIBRARIES}
)

INSTALL(TARGETS fopbc DESTINATION bin)
